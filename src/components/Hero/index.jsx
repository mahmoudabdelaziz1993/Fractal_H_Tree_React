import React from "react";
import "./hero.css";
const Hero = () => (
  <div className="hero">
    <h3>Fractal H Tree</h3>
    <p>
      the H tree is a fractal tree structure constructed from perpendicular line
      segments, each smaller by a factor of the square root of 2 from the next
      larger adjacent segment. It is so called because its repeating pattern
      resembles the letter "H".{" "}
    </p>
  </div>
);
export default Hero;
